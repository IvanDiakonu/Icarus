package Icarus;

public class incrementOperatorPractice {
	
	public static void main(String args[])  {
		
		int tuna = 5;
		int bass = 18;
		//(below) "++" changes the value of the variable by 1.
		// "--" subtracts the value of the variable by 1.
		// "(variable) + (any number)" will let you add any number to the existing variable.
		// "(variable) += (any number) will add the number you input to the existing variable, and change it completely.
				// You can alter the +=, to anything (e.g /=, *=)
				//this is the assignment operator
		tuna++;
		System.out.println(tuna);
	}

}
